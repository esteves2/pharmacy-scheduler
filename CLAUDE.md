## Rules

- Every assumption or decision not explicitly approved by Martim must be saved to a markdown file under `decisions/`.
- Never run any git command without being explicitly asked.
- Never commit, stage, push, or modify git config without Martim confirming first.
